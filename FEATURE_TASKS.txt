================================================================================
SAVIO — РЕЕСТР ЗАДАЧ НА НОВЫЙ ФУНКЦИОНАЛ
================================================================================
Составлено по результатам изучения кода (все 4 слоя) и сверки с CLAUDE.md.
Дата: 2026-07-21.

Каждая задача привязана к реальным точкам расширения в коде и НЕ ломает границы
слоёв. Объём: S (несколько часов) / M (день-два) / L (несколько дней).

Общее правило готовности для ВСЕХ задач (Правило 2 CLAUDE.md):
    cargo build
    cargo clippy --all-targets -- -D warnings
    cargo test
    поднять version в Cargo.toml (см. semver в задаче)
    сверить README.md и CLAUDE.md (Правило 5)
Для UI-задач вдобавок — глазной прогон состояний (покой, нет yt-dlp, невалидная
ссылка, загрузка, готово, отмена) и минимальный размер окна 520×420.

--------------------------------------------------------------------------------
СВОДНАЯ ТАБЛИЦА
--------------------------------------------------------------------------------
 #  Задача                                             Объём  В планах README
 1  Кнопка «Скопировать журнал»                        S      нет
 2  Понятные ошибки 403 / гео / возраст / бот          S      нет
 3  Форматтер скорости + единица ТБ                    S      нет
 4  Выбор качества видео / битрейта аудио              M      ДА
 5  Превью обложки ролика до загрузки                  M      нет
 6  Флажки «вшить метаданные / обложку / субтитры»     M      нет
 7  Запоминание формата и папки между запусками        M      нет
 8  Cookies из браузера для закрытых роликов           M      нет
 9  Вкладка «История загрузок»                         M      ДА
10  Обрезка по времени (фрагмент)                      M      нет
11  Очередь загрузок (пакет ссылок)                    L      ДА
12  Показ версий и обновление ffmpeg                   L      нет
13  Паника wgpu на окне шире/выше 8192                 S      нет
14  «Отмена» в первые секунды не отменяет загрузку     S      нет
15  Превью и название до нажатия «Скачать»             L      нет
16  Автоматические субтитры и выбор их языка           M      нет
17  Вкладка «Проверка состояния системы»               L      нет
18  Монитор производительности и игровой оверлей       L      нет
19  Управление режимами питания и производительности   L      нет
20  Запоминать и галочки «вшить в файл»                S      нет
21  История на диске + кнопка «Очистить»               M      нет
22  Двойная подсказка у обрезанных подписей  ⚠дефект   S      нет
23  Файл cookies.txt как второй источник входа         S      нет
24  Запоминать ли выбранный источник cookies           S      нет

Рекомендация по порядку: начать с #4 (самое востребованное, уже в планах, данные
для него уже скачиваются `probe()` впустую). Мелочь #1 и #2 — заметная польза
примерно за час каждая.



################################################################################
ЗАДАЧА 10: Скачивание фрагмента по времени (обрезка)
################################################################################
Объём:            M
Приоритет:        ниже среднего
В планах README:  нет
Слои/файлы:       src/model.rs (парсер времени рядом с human_duration + #[test]),
                  src/engine/ytdlp.rs (download_args --download-sections),
                  src/app.rs (два поля ввода)

ЗАЧЕМ
  Пользователь берёт из длинного видео только нужный кусок (например, 1:30–4:00),
  экономя время и место.

ЧТО СДЕЛАТЬ
  Нести в запросе необязательные start/end в секундах и выдавать их в
  download_args как `--download-sections "*{start}-{end}"`. В model.rs добавить
  чистый парсер «1:30» → секунды (обратный к human_duration) с #[test]. UI — два
  текстовых поля.

ТОЧКИ РАСШИРЕНИЯ
  Парсер времени рядом с human_duration (model.rs), download_args (ytdlp.rs:84),
  download_tab (app.rs).

СОБЛЮДЕНИЕ АРХИТЕКТУРЫ
  Разбор и форматирование времени — чистый домен; сборка CLI в движке; UI только
  вводит строки, ничего не парсит.

РИСКИ И ЛОВУШКИ
  Rule-6-подобное: `--download-sections` требует ffmpeg и при синтаксической
  ошибке молча качает весь файл вместо фрагмента — валидировать диапазон в
  доменном парсере и требовать tools.ffmpeg, иначе Warning. Пустой/перевёрнутый
  диапазон отсекать ДО запуска.

ГОТОВНОСТЬ
  [ ] build / clippy / test (+ #[test] на парсер времени, вкл. перевёрнутый диапазон)
  [ ] версия: minor
  [ ] README: обновить (новая возможность)
  [ ] CLAUDE.md: не требуется
  [ ] UI: поля ввода, невалидный диапазон, поведение без ffmpeg


################################################################################
ЗАДАЧА 11: Очередь загрузок (пакет из нескольких ссылок)
################################################################################
Объём:            L
Приоритет:        крупная, отложенная
В планах README:  ДА («Одновременная загрузка нескольких файлов»)
Слои/файлы:       src/model.rs (Progress.download_id, Done/Failed несут id,
                  State::Queued),
                  src/engine/mod.rs (start/run принимают id, метят события),
                  src/app.rs (ограниченный queue: Vec, State, все match)

ЗАЧЕМ
  Пользователь ставит десяток ссылок в очередь и уходит — они качаются по одной
  без ручного перезапуска после каждой.

ЧТО СДЕЛАТЬ
  Завести в SavioApp ограниченное `queue: Vec<QueueItem>` (потолок в духе
  LOG_LIMIT) и состояние `State::Queued`. Чтобы развести события нескольких
  дочерних yt-dlp по одному каналу Event, добавить `download_id: u32` в Progress
  (он Copy — тип обязан остаться Copy) и нести id в Done/Failed, РАСШИРЯЯ варианты,
  а не заводя второй канал. engine::start получает id, run() (mod.rs) метит каждое
  событие.

ТОЧКИ РАСШИРЕНИЯ
  Progress (model.rs:405), Event Done/Failed (model.rs:450-451), enum State
  (app.rs:57), engine::start + Handle (mod.rs:53,35), can_start()/start()
  (app.rs:564/571), поле queue в SavioApp (app.rs:322).

СОБЛЮДЕНИЕ АРХИТЕКТУРЫ
  Единственный канал Event сохраняется, движок остаётся CLI-пригодным. State/Event
  расширяются, а не переосмысляются; очередь несёт предел памяти по Правилу 1.

РИСКИ И ЛОВУШКИ
  Смена Done(PathBuf) на структуру с id заденет ВСЕ исчерпывающие match — это и
  есть задуманная компилятором работа, но объёмная (по всему app.rs). Блокирующая
  ловушка: никаких join потоков на UI; каждая загрузка — свой поток; не плодить
  потоки без предела — запускать строго по одному из очереди.

ГОТОВНОСТЬ
  [ ] build / clippy / test
  [ ] версия: minor (заметная возможность)
  [ ] README: обновить (раздел «Использование», пункт планов вычеркнуть)
  [ ] CLAUDE.md: возможно правило про id-маршрутизацию событий одного канала
  [ ] UI: очередь пустая/в работе/с ошибкой отдельного элемента, отмена, 520×420


################################################################################
ЗАДАЧА 12: Показ версий и обновление ffmpeg
################################################################################
Объём:            L
Приоритет:        крупная, отложенная
В планах README:  нет
Слои/файлы:       src/engine/setup.rs (ffmpeg_version, start_update/run_update/
                  Outcome для ffmpeg),
                  src/engine/binaries.rs (Tools.ffprobe),
                  src/app.rs (maintenance_row)

ЗАЧЕМ
  Пользователь видит установленные версии yt-dlp и ffmpeg и может обновить ffmpeg
  тем же способом, что и движок, не скачивая архивы вручную.

ЧТО СДЕЛАТЬ
  Добавить `ffmpeg_version()` и по образцу setup::start_update/run_update/enum
  Outcome сделать ветку обновления ffmpeg (перекачка «latest»-сборки поверх копии
  Origin::Owned), показывая обе версии в maintenance_row. Заодно добавить
  `ffprobe: Option<PathBuf>` в Tools и заполнять в discover(), чтобы метаданные
  перестали каждый раз заново искать ffprobe.

ТОЧКИ РАСШИРЕНИЯ
  ffmpeg_version() по образцу ytdlp_version() (setup.rs:478),
  start_update/run_update/enum Outcome (setup.rs:514-621),
  Tools (binaries.rs:100) + discover() (binaries.rs:159),
  maintenance_row (app.rs:1661).

СОБЛЮДЕНИЕ АРХИТЕКТУРЫ
  Обновляется ТОЛЬКО Origin::Owned (системный ffmpeg пользователя выигрывает);
  переиспользуются Event::Notice/Warning; запросы к чужому серверу повторяются
  через REQUEST_ATTEMPTS.

РИСКИ И ЛОВУШКИ
  Двойная ловушка Правила 6: архив ffmpeg сейчас НЕ проверяется контрольной
  суммой, а `tar` возвращает 0, даже распаковав ноль файлов. Обязательно сохранить
  пост-распаковочные is_file()-проверки и по-хорошему добавить сверку sha256.
  Разбор `ffmpeg -version` разнится между сборками — парсить терпимо, с фолбэком.
  На Windows звать системный tar по абсолютному %SystemRoot%\System32\tar.exe.

ГОТОВНОСТЬ
  [ ] build / clippy / test (+ #[test] на парсер версии ffmpeg)
  [ ] версия: minor
  [ ] README: обновить (обслуживание, обновление ffmpeg)
  [ ] CLAUDE.md: возможно строка про sha256 для архива ffmpeg
  [ ] UI: maintenance_row с версиями, ход обновления, отсутствие интернета


################################################################################
ЗАДАЧА 13: Паника wgpu на окне шире/выше 8192   ⚠ дефект, не функционал
################################################################################
Объём:            диагностика сделана 2026-07-26; своими силами не чинится
Приоритет:        низкий по частоте, высокий по последствиям
В планах README:  нет
Слои/файлы:       дефект не в коде Savio. Наши файлы затронуты не будут:
                  проверено, что ни main.rs, ни app.rs повлиять на это не могут.

ЗАЧЕМ
  Приложение падает целиком, теряя идущую загрузку, и в релизе падает МОЛЧА:
  консоль скрыта (`windows_subsystem = "windows"`, main.rs:2), поэтому текст
  паники пользователю не достаётся вообще — окно просто исчезает. Проверено на
  РЕЛИЗНОЙ сборке: падает слово в слово так же, как в отладочной.

ПРИЧИНА (установлена, прежнее объяснение в этой задаче было неверным)
  Клиентская часть окна — это текстура wgpu. egui-wgpu просит у устройства
  ровно `max_texture_dimension_2d: 8192` — жёстко зашитым числом, независимо от
  возможностей видеокарты (egui-wgpu-0.35.0/src/setup.rs:265-269). А поверхность
  настраивается сырым размером окна, без ограничения сверху
  (egui-wgpu-0.35.0/src/winit.rs:135-137), хотя предел тут же и доступен —
  `Painter::max_texture_side()` в том же файле, строки 318-322. Как только
  клиент окна выходит за 8192 физических пикселя по любой стороне,
  `Surface::configure` роняет процесс паникой.

  Граница ровная, гонки нет, воспроизводится всегда:
      внешняя высота 8231 → клиент 504×8192 → работает;
      внешняя высота 8232 → клиент 504×8193 → паника;
      внешняя ширина 10000 → клиент 9984×381 → паника (обе стороны, не только высота).

  Откуда в прошлом отчёте взялось 32728: Windows обрезает высоту окна по
  SHRT_MAX = 32767, минус 39 пикселей неклиентской части = 32728. То есть в том
  прогоне до MoveWindow дошла высота ≥ 32767, а не 420 — записанные аргументы
  вызова такой паники дать не могли. Ширина 504 при этом настоящая (520 − 16).

  ЧТО ОКАЗАЛОСЬ НЕ ПРИ ЧЁМ (проверено, не предполагается):
  - Размер НИЖЕ минимума. Клиент 504×381 при заявленном минимуме 520×420 —
    24 прогона, ни одного падения. Windows действительно не соблюдает
    `min_inner_size` при программном ресайзе, но это безобидно.
  - Гонка с незаконченным SW_RESTORE. Прогнано порознь: корректный размер без
    паузы, размер ниже минимума с паузой 2 с, 120 быстрых ресайзов подряд,
    40 циклов «развернуть/восстановить» — всё живо.
  - Наш `maximize_pending`. С `maximize_pending: false`, на окне 736×599,
    которое никто не разворачивал, падает точно так же.
  - Свёрнутое окно (клиент 0×0) — eframe его отсеивает, паники нет.

ЧТО СДЕЛАТЬ
  Сообщение вверх по течению в emilk/egui: egui-wgpu обязан ограничивать размер
  поверхности пределом устройства, значение для этого у него уже под рукой.
  Своими силами не обходится, проверено:
  - `with_max_inner_size([1200,800])` НЕ помогает — падает слово в слово. winit
    выставляет пределы только в `WM_GETMINMAXINFO`, а Windows применяет их к
    перетаскиванию рамки и развороту, но не к `SetWindowPos`/`MoveWindow`.
  - Поднять запрошенный предел через `NativeOptions::wgpu_options` до предела
    видеокарты (обычно 16384) можно, но это не починка, а перенос потолка:
    паника останется, просто дальше. Ровно та подгонка, от которой этот файл
    предостерегает.
  - Смена рендерера на glow убирает валидацию wgpu целиком, но меняет отрисовку
    и требует полного глазного прогона; на дефект такой цены нет.

ЛОВУШКА ДЛЯ ПРОВЕРЯЮЩЕГО
  `(Get-Process savio).MainWindowHandle` в первые секунды отдаёт СКРЫТОЕ
  служебное окно winit 16×16 с тем же заголовком, а не настоящее. Ресайз уходит
  в никуда, и прогон «не падает» ничего не доказывает. Настоящее окно искать
  через EnumWindows по наибольшему клиентскому прямоугольнику процесса.

ГОТОВНОСТЬ
  [ ] сообщение заведено вверх по течению (emilk/egui)
  [ ] версия: не трогать — в коде Savio правок нет
  [ ] README: не требуется (пользовательских изменений нет)


################################################################################
ЗАДАЧА 14: «Отмена» в первые секунды не отменяет загрузку  ⚠ дефект, не функционал
################################################################################
Объём:            S
Приоритет:        средний
В планах README:  нет
Слои/файлы:       src/engine/mod.rs (Handle, start, run), src/app.rs (cancel)
Найдено:          при задаче 5 (превью обложки), 2026-07-25

ЗАЧЕМ
  Пользователь нажимает «Отмена» в первые секунды после «Скачать», окно честно
  показывает «Отменено» — а файл всё равно докачивается и появляется в папке.
  Хуже тихой ошибки: приложение сообщает об одном, а делает другое.

ЧТО ПРОИСХОДИТ
  Handle::cancel() (mod.rs:42) умеет ровно одно — убить процесс из child_slot.
  Но пока идёт probe() (mod.rs:315, отдельный процесс, в слот он не попадает),
  слот пуст: убивать нечего, и cancel() молча ничего не делает. Поток движка
  спокойно доходит до конца probe, спавнит yt-dlp и качает файл целиком. UI при
  этом уже в State::Cancelled и события игнорирует (app.rs:619 сбрасывает rx),
  так что расхождение видно только по появившемуся файлу.

  Окно длится столько, сколько идёт probe: 1–3 секунды, на медленном канале
  дольше. Загрузка обложки, добавленная задачей 5, его НЕ расширяет — перед
  запросом за картинкой проверяется, жив ли приёмник (mod.rs:118), и после
  отмены запроса уже не будет. Сам probe этой проверки не имеет.

ЧТО СДЕЛАТЬ
  Завести в Handle общий `Arc<AtomicBool>` — ровно так, как это уже сделано
  в setup::Handle (setup.rs:198), там та же задача решена и приём проверен.
  Смотреть флаг после probe и непосредственно перед spawn; при отмене выходить
  из run() без запуска yt-dlp и без Event::Failed. Убийство процесса из
  child_slot оставить как есть — оно обслуживает уже идущую загрузку.

ТОЧКИ РАСШИРЕНИЯ
  Handle (mod.rs:35), cancel (mod.rs:42), start (mod.rs:53), run (mod.rs:97),
  probe (mod.rs:315), setup::Handle как образец (setup.rs:198).

  ВНИМАНИЕ: probe теперь принимает весь `Request`, а не одну ссылку — так его
  изменила задача 8 (cookies), которым тоже надо доехать до запроса метаданных.

СОБЛЮДЕНИЕ АРХИТЕКТУРЫ
  Флаг живёт внутри движка, наружу торчит прежний Handle::cancel(). Канал
  событий, домен и UI не меняются: приём тот же, что у установки.

РИСКИ И ЛОВУШКИ
  - Одной проверки в начале run() мало: между ней и spawn остаётся вся длина
    probe. Проверять непосредственно перед запуском процесса.
  - Event::Failed при отмене слать нельзя: по Правилу 2 UI обязан показать
    «Отменено», а не ошибку.
  - Тестом не ловится — это гонка. Проверять живьём: нажать «Отмена» в первую
    секунду и убедиться, что в папке НЕ появился файл. Код возврата и «вроде
    отменилось» ничего не доказывают.
  - probe запускается и при отменённой установке инструментов — заодно
    проверить, что флаг не ломает этот путь.

ГОТОВНОСТЬ
  [ ] build / clippy / test
  [ ] версия: patch
  [ ] README: не требуется
  [ ] CLAUDE.md: не требуется
  [ ] UI: отмена в первую секунду (файла в папке быть не должно), отмена
      в середине загрузки (прежнее поведение), отмена после «Готово»


################################################################################
ЗАДАЧА 15: Превью и название до нажатия «Скачать»
################################################################################
Объём:            L
Приоритет:        средний
В планах README:  нет
Слои/файлы:       src/engine/mod.rs (отдельный пуск probe в потоке),
                  src/app.rs (url_field: дебаунс, свой приёмник, поколение)
Найдено:          при задаче 5 (превью обложки), 2026-07-25

ЗАЧЕМ
  Задача 5 научила Savio показывать обложку, название, автора и длительность —
  но всё это приезжает уже после нажатия «Скачать». Ради чего превью и
  задумывалось — убедиться, что в буфере оказалась та самая ссылка, — остаётся
  недоделанным: к моменту показа загрузка идёт, и «не тот ролик» приходится
  отменять, а не предотвращать.

ЧТО СДЕЛАТЬ
  Пускать probe по изменению поля «Ссылка», с дебаунсом около 0.8 с, отдельным
  потоком и отдельным каналом Event — тем же, что уже принимает Info и
  Thumbnail. Загрузка при этом ничего не теряет: свой probe у неё остаётся,
  либо (лучше) переиспользует уже полученный результат, если ссылка та же.

ТОЧКИ РАСШИРЕНИЯ
  url_field (app.rs:1177) — там уже есть response.changed(), то есть точка,
  где текст менялся, а не проверялся в кадре; drain_events (app.rs:738);
  probe (mod.rs:315) — сделать pub и завернуть в пуск потока рядом со start.
  Учесть, что probe принимает `Request` целиком (ему нужны cookies): для
  предпросмотра до нажатия «Скачать» запрос придётся собирать из текущего
  выбора в UI, иначе закрытый ролик не покажет ни названия, ни обложки.

СОБЛЮДЕНИЕ АРХИТЕКТУРЫ
  Канал остаётся единственным средством связи: новый поток шлёт те же Info и
  Thumbnail. Движок по-прежнему не знает про egui. Домен не меняется вовсе.

РИСКИ И ЛОВУШКИ
  - Заметное изменение поведения: сетевой запрос уходит на сайт до того, как
    пользователь нажал хоть что-то. Это стоит сказать в README, а возможно — и
    дать выключатель: человек мог вставить ссылку, чтобы просто её посмотреть.
  - Дебаунс обязателен, иначе probe запускается на каждый набранный символ, и
    сайт получает десяток запросов на одну ссылку — прямая дорога к «Sign in to
    confirm you're not a bot», причём заработанная на ровном месте.
  - Гонка ответов: probe по старой ссылке возвращается позже, чем по новой, и
    в окне оказывается название чужого ролика. Нужен счётчик поколений —
    ответ с устаревшим номером выбрасывается. Компилятор такого не ловит.
  - Не запускать probe на то, что не похоже на ссылку (looks_like_url уже есть),
    иначе каждый обрывок текста из буфера уходит в yt-dlp.
  - Идущая загрузка не должна мешаться с фоновым probe: у них разные приёмники
    (как у вкладки «Метаданные»), иначе события двух задач попадут в один сток.
  - Отмена: probe по устаревшей ссылке надо уметь бросать, иначе на медленном
    сайте фоновые процессы накопятся десятками.

ГОТОВНОСТЬ
  [ ] build / clippy / test (+ тест на отбрасывание ответа устаревшего поколения)
  [ ] версия: minor
  [ ] README: обновить (запрос уходит при вставке ссылки — заметно пользователю)
  [ ] CLAUDE.md: возможно строка — фоновый запрос по вводу требует дебаунса и
      поколений; без них это лишние запросы и чужое название в окне
  [ ] UI: вставка валидной ссылки, быстрая замена одной ссылки другой, мусор
      в поле, вставка во время идущей загрузки, 520×420



################################################################################
ЗАДАЧА 16: Автоматические субтитры и выбор их языка
################################################################################
Объём:            M
Приоритет:        средний
В планах README:  нет
Слои/файлы:       src/model.rs (DownloadOptions:218, MediaInfo.has_subtitles:263),
                  src/engine/ytdlp.rs (download_args:69, parse_has_subtitles:375),
                  src/app.rs (embed_options:1125)

ЗАЧЕМ
  Галочка «Субтитры» из задачи 6 берёт только те субтитры, которые выложил автор
  ролика. На YouTube их сегодня почти ни у кого нет: пользовательские переводы
  свернули, а осталось распознавание речи — automatic_captions. То есть у
  большинства роликов галочка честно сообщает «вшивать нечего» и ничего не
  делает, хотя текст для вшивания на сайте лежит. Человеку, которому субтитры
  нужны как костыль (шумно, чужой язык, плохой звук), робот-распознаватель
  подходит вполне — а взять его сейчас нечем.

ЧТО СДЕЛАТЬ
  Добавить в DownloadOptions поле auto_subs (или сделать subs тремя
  состояниями: нет / свои / свои и автоматические) и ключ --write-auto-subs.
  Вместе с ним обязателен явный --sub-langs: язык надо выбирать, а не
  получать по умолчанию (см. ловушки). В UI — второй флажок «Можно
  автоматические» под «Субтитрами», активный только когда те включены,
  и выпадающий список языков из ответа probe.

ТОЧКИ РАСШИРЕНИЯ
  download_args (ytdlp.rs:84) — ветка `if options.embed_subs`;
  parse_has_subtitles (ytdlp.rs:455) — рядом читать automatic_captions;
  MediaInfo (model.rs:335) — список доступных языков вместо голого bool;
  embed_options (app.rs:1298) — второй флажок и список;
  cookie_selector (app.rs:1378) — готовый образец выпадающего списка
  в теме Savio, вместе с потолком высоты раскрытого меню.

СОБЛЮДЕНИЕ АРХИТЕКТУРЫ
  Опции остаются плоскими данными, сборка CLI — в download_args, разбор JSON —
  в движке. Нового канала не появляется, домен чист.

РИСКИ И ЛОВУШКИ
  - Главная ловушка — язык. YouTube кладёт в automatic_captions не только
    распознанный язык, но и машинный перевод на сотню других, и `en` есть
    почти всегда. Без явного --sub-langs yt-dlp берёт `en` первым — и в
    русский ролик вшивается английский машинный перевод русского
    распознавания. Ошибки не будет ни в сборке, ни в коде возврата: субтитры
    появятся, просто не те. Проверять надо глазами, на русском ролике.
  - Качество распознавания заметно хуже авторских субтитров, и это стоит
    сказать в UI, а не выяснять по жалобам. По той же причине автоматические
    не должны включаться сами: галочка обязана быть отдельной.
  - Авторские субтитры приоритетнее автоматических: если у ролика есть и те и
    другие, брать надо авторские. yt-dlp так и делает, но только когда
    включены оба ключа сразу (--embed-subs подразумевает свои субтитры,
    --write-auto-subs добавляет автоматические). Включить один
    --write-auto-subs без --write-subs — значит выключить авторские совсем.
  - Список языков из probe придётся показывать в UI: сейчас оттуда берётся
    только bool. Собирать список в кадре отрисовки нельзя (Правило 1) — как
    и с heights, готовить его надо в движке.

ГОТОВНОСТЬ
  [ ] build / clippy / test (+ тест на выбор языка и на приоритет авторских)
  [ ] версия: minor
  [ ] README: обновить (появился второй флажок и выбор языка)
  [ ] CLAUDE.md: возможно строка — про `en` по умолчанию в automatic_captions
  [ ] UI: ролик со своими субтитрами, ролик только с автоматическими, ролик
      без всяких, переключение MP4/MP3, 520x420



################################################################################
ЗАДАЧА 17: Вкладка «Проверка состояния системы»
################################################################################
Объём:            L (по шкале реестра — верх; по факту недели, а не дни)
Приоритет:        сначала решение человека, см. ЗАЧЕМ
В планах README:  нет
Слои/файлы:       новый src/engine/hardware/ (сбор снимка),
                  src/model.rs (типы отчёта, Event:344),
                  src/app.rs (Tab:86, tab_bar:788, новая health_tab)

ЗАЧЕМ
  Пользователь получает диагностику компьютера: процессор, память, диски и их
  SMART, батарея с износом, сетевые адаптеры, USB, аудио- и видеовыходы,
  встроенные и подключённые устройства — со статусом, причиной и советом.

  Но первый вопрос здесь не технический. Savio по README — «приложение для
  скачивания видео и аудио», обёртка над yt-dlp и ffmpeg. Состояние батареи и
  SMART накопителя к загрузке отношения не имеют: это отдельный продукт
  (HWiNFO, CrystalDiskInfo), которому предлагается жить в чужом окне. Прежде
  чем писать код, надо ответить: Savio — загрузчик или набор утилит для
  компьютера? От ответа зависят README, название и то, оправданы ли риски ниже.
  Вкладка «Метаданные» шаг в ту же сторону уже сделала, но она хотя бы про файлы.

ЧТО СДЕЛАТЬ
  Движок собирает снимок одной структурой: HealthReport { checks: Vec<Check> },
  Check { name, status (Ок / Предупреждение / Ошибка), detail, advice }. Сбор —
  в отдельном потоке, наружу приезжает готовый отчёт новым вариантом Event.
  UI рисует общий итог и список карточек, кнопку «Проверить снова» и сохранение
  отчёта в файл (rfd уже в зависимостях).

  Делить на два этапа. Первый — то, что читается без прав администратора и
  одинаково на трёх системах: процессор, память, диски и их объём, сетевые
  адаптеры, перечень подключённых устройств. Второй — SMART, температуры,
  обороты вентиляторов, износ батареи: у каждого своя цена, см. ловушки.

ТОЧКИ РАСШИРЕНИЯ
  Tab (app.rs:95) — очередной вариант перечисления;
  tab_bar (app.rs:944) — деление ширины на 2 убрано задачей 9: сегменты уже
  считаются по числу вкладок, и новую достаточно дописать строкой в TABS;
  Event (model.rs:433) — новый вариант под отчёт;
  start_metadata (mod.rs:276) и MetaPanel (app.rs:184) — готовый образец
  фоновой задачи со своим приёмником, копировать надо его.

СОБЛЮДЕНИЕ АРХИТЕКТУРЫ
  Слой «Движок» в таблице CLAUDE.md описан как «запуск процессов, разбор
  вывода, сеть» — опроса железа там нет, строку в таблицу придётся дописать.
  Сбор обязан идти в потоке: ни WMI, ни чтения /sys в ui() быть не должно.
  Зависимости — отдельный разговор. «Не добавлять зависимость, если задача
  решается стандартной библиотекой» здесь неприменимо: не решается ничем.
  Минимум — sysinfo; SMART, температуры, GPU и батарея тянут ещё по крейту на
  платформу. Нынешний набор из пяти вырастет вдвое, и это решение о продукте.

РИСКИ И ЛОВУШКИ
  - Проверено вживую (Windows 11, обычные права, 2026-07-26):
        Win32_TemperatureProbe          16 объектов, CurrentReading пуст
        Win32_Fan                       5 объектов, DesiredSpeed пуст
        MSAcpi_ThermalZoneTemperature   пусто
        MSStorageDriver_FailurePredict* «Access denied»
        Win32_Battery                   пусто (настольная машина)
    Штатный путь Windows к температурам и вентиляторам не отказывает — он
    отдаёт объекты без значений. Ровно тот молчаливый провал, ради которого
    написано Правило 6: увидеть «объекты есть» и отчитаться об успехе легко,
    проверять надо наличие значения.
  - Температуры и обороты на Windows без своего драйвера не достать в
    принципе: LibreHardwareMonitor и подобные ставят WinRing0, ему нужны
    права администратора, и антивирусы его метят. Savio заявлен как
    работающий без прав администратора и уже страдает от ложных срабатываний
    на yt-dlp.exe — второй повод для антивируса дороже этой функции.
  - SMART без администратора недоступен (см. «Access denied» выше). Либо
    вкладка честно просит права, либо пункт выпадает. Врать «диск здоров»,
    когда статус не прочитан, нельзя: это худший вид молчаливого отказа.
  - Кроссплатформенность (Правило 2): SMART, дерево USB, батарея и GPU
    читаются на трёх системах тремя разными способами. Это три реализации на
    пункт, а не одна с ветвлением, и проверить их локально нельзя — только CI.
  - «Рекомендации по устранению» — самое опасное место. Совет не про ту беду
    хуже отсутствия совета: тот же довод, что записан у explain_failure.
    Держать советы для узнаваемых случаев, в остальных молчать.
  - Постановка требует проверять разъёмы: USB 2.0 / 3.x / Type-C, AUX, HDMI
    Audio, DP Audio, VGA, DVI. Система такого не сообщает — видно устройство
    и его драйвер, а какой разъём распаян на плате, нет. Пункт надо
    переформулировать в «перечислить выходы, которые видит система», иначе
    он невыполним по формулировке, а не по сложности.

ГОТОВНОСТЬ
  [ ] решение человека: расширяем ли продукт (см. ЗАЧЕМ) — до всякого кода
  [ ] build / clippy / test (+ тесты на разбор снимка: «нет значения» ≠ «ноль»)
  [ ] версия: minor
  [ ] README: новая вкладка и новые требования к правам
  [ ] CLAUDE.md: строка в таблицу слоёв про опрос железа; строка в Правило 6
      про WMI, отдающий объекты без значений
  [ ] UI: машина без батареи, без дискретной GPU, без прав администратора;
      520x420; третья вкладка в дорожке (ширина сейчас делится на 2)


################################################################################
ЗАДАЧА 18: Монитор производительности и игровой оверлей
################################################################################
Объём:            L, и это две разные задачи: монитор и оверлей
Приоритет:        зависит от решения по задаче 17
В планах README:  нет
Слои/файлы:       новый src/engine/monitor.rs (опрос),
                  src/model.rs (Event:344), src/app.rs (Tab:86, tab_bar:788),
                  src/main.rs (второй вьюпорт под оверлей)

ЗАЧЕМ
  Пользователь видит загрузку CPU, GPU, памяти, дисков и сети в реальном
  времени, список процессов по потреблению — и то же самое поверх игры, не
  сворачивая её.

  Оговорка из задачи 17 действует и здесь: это не про скачивание. Оверлей
  вдобавок — самостоятельный продукт (MSI Afterburner, RTSS), а не вкладка.

ЧТО СДЕЛАТЬ
  Поток-опросчик с интервалом около секунды, свой приёмник Event (как у
  вкладки «Метаданные»), готовые строки собираются при получении данных, а не
  в кадре: список процессов — это сотни строк, и format! по ним 60 раз в
  секунду недопустим (Правило 1). Кадры просить через request_repaint_after:
  без ввода egui окно не перерисовывает.
  Оверлей — отдельный вьюпорт egui (show_viewport_deferred с ViewportBuilder:
  without decorations, transparent, always on top, mouse passthrough), а не
  второе приложение и не второй процесс.

ТОЧКИ РАСШИРЕНИЯ
  Tab (app.rs:95), tab_bar (app.rs:944) — вкладка дописывается строкой в TABS,
  ширину сегментов цикл пересчитает сам (деление на 2 убрано задачей 9);
  MetaPanel (app.rs:184) — образец панели со своим каналом и drain;
  start_metadata (mod.rs:276) — образец фоновой задачи.

СОБЛЮДЕНИЕ АРХИТЕКТУРЫ
  Канал остаётся единственной связью движка с UI. Опрос железа — та же новая
  строка в таблице слоёв, что и в задаче 17: делать обе имеет смысл на одном
  сборщике, иначе появятся два разных способа спросить загрузку процессора.

РИСКИ И ЛОВУШКИ
  - Любой накопитель ограничен по размеру — это уже правило (LOG_LIMIT).
    История для графиков обязана быть кольцевым буфером фиксированной длины,
    иначе оставленный на ночь монитор съест память.
  - Сейчас Savio в покое не тратит ни кадра. Секундный опрос означает
    постоянную перерисовку, в том числе у свёрнутого окна. Нужен явный
    останов опроса, когда вкладка не видна и оверлей выключен, — иначе
    загрузчик начнёт греть ноутбук в фоне.
  - FPS чужого приложения со стороны не измеряется. Нужен перехват Present
    (PresentMon, ETW), на Windows — с правами администратора. Пункт «FPS
    (если доступно)» либо вычеркнуть, либо выносить в отдельную задачу: иначе
    он утащит за собой всю остальную работу.
  - Оверлей поверх игры виден только в оконном и безрамочном режимах. В
    эксклюзивном полноэкранном обычное окно не показывается никогда — туда
    рисуют только перехватом графического API. Сказать это в UI обязательно,
    иначе «оверлей не работает» станет главным отзывом, и справедливо.
  - Потребление на процесс: CPU, память и диск даёт sysinfo; GPU на Windows —
    только счётчики «GPU Engine» через PDH; сеть на процесс кроссплатформенно
    не даётся вовсе (нужен ETW). Колонка, пустая на половине систем, — тот же
    молчаливый провал, что и «Температура: —».
  - Прозрачное окно и wgpu: дефект 13 показывает, что геометрия окна в текущей
    связке уже роняет приложение целиком. Второй вьюпорт — новая площадь для
    того же класса падений, и падение оверлея убьёт идущую загрузку.

ГОТОВНОСТЬ
  [ ] решение человека по задаче 17 (тот же вопрос о продукте)
  [ ] build / clippy / test (+ тест на предел истории показателей)
  [ ] версия: minor
  [ ] README: новая вкладка, оверлей и его ограничение по полноэкранным играм
  [ ] CLAUDE.md: строка про останов опроса, когда вкладка не видна
  [ ] UI: одна и несколько GPU, ноутбук и настольная машина, свёрнутое окно,
      оверлей поверх оконной и полноэкранной игры, 520x420


################################################################################
ЗАДАЧА 19: Управление режимами питания и производительности
################################################################################
Объём:            L
Приоритет:        низкий: реалистичная часть здесь мала, см. ловушки
В планах README:  нет
Слои/файлы:       src/engine/power.rs (чтение и применение режима),
                  src/model.rs (Event:344), src/app.rs (вкладка из задачи 18)

ЗАЧЕМ
  Пользователь переключает режим питания и профили производительности прямо из
  Savio, не открывая параметры Windows.

ЧТО СДЕЛАТЬ
  Показать текущий режим питания и дать его переключать. Всё остальное из
  постановки — профили CPU и GPU, режим вентиляторов, параметры накопителей —
  сначала проверить на достижимость, см. ловушки: реалистичной остаётся, судя
  по всему, только схема питания.

ТОЧКИ РАСШИРЕНИЯ
  Вкладка монитора из задачи 18; Event (model.rs:433) — сообщение о
  применённом режиме; start_metadata (mod.rs:276) — образец фоновой задачи
  (переключение схемы уходит в поток, в ui() запускать процессы нельзя).

СОБЛЮДЕНИЕ АРХИТЕКТУРЫ
  Через powercfg это обёртка над внешней программой — ровно то, что Savio уже
  умеет, и слои не ломаются. Через powrprof.dll это первый в проекте вызов
  системного API напрямую: понадобится либо windows-crate, либо ручные extern.

РИСКИ И ЛОВУШКИ
  - Проверено вживую (Windows 11, обычные права, 2026-07-26):
    `powercfg /getactivescheme` и `/list` читаются без администратора, но
    показывают три классические схемы — Balanced, High performance, Power
    saver. Режимов из постановки («Best Performance», «Battery Saver») среди
    них нет вовсе: в Windows 11 ползунок «Режим питания» переключает не схему,
    а overlay-схему, и powercfg о ней не знает — `/overlaylist` отвечает
    «Invalid Parameters». Добраться до неё можно только недокументированными
    PowerGetEffectiveOverlayScheme / PowerSetActiveOverlayScheme из
    powrprof.dll. Показать вместо ожидаемых названий три powercfg-схемы —
    значит показать список, которого пользователь в своей системе не видел.
  - «Применять сразу и не спрашивать» разумно для схемы питания и опасно для
    оборотов вентилятора: там неверное значение кончается перегревом. Savio
    уже переспрашивает перед необратимым (модалка очистки метаданных) — та же
    мера нужна всему, что трогает охлаждение.
  - Вентиляторы и профили CPU/GPU идут через EC и вендорские SDK (NVAPI, ADL,
    драйверы конкретных ноутбуков): права администратора, свой драйвер, разное
    поведение на каждой модели. Трезвая оценка — не делать вовсе.
  - Кроссплатформенность: аналога powercfg на Linux и macOS нет, cpufreq/TLP и
    pmset — другие сущности с другим смыслом. Вкладка выйдет Windows-only, а
    Правило 2 требует пары к каждой ветке #[cfg]. Нужна честная заглушка «на
    этой системе недоступно».
  - «Отображать только поддерживаемое» проверяется попыткой, а не наличием
    API: функция может существовать, вернуть успех и ничего не изменить
    (Правило 6). После применения — перечитать текущий режим и сверить с
    запрошенным, иначе переключатель будет врать.

ГОТОВНОСТЬ
  [ ] разведка: достижимы ли overlay-схемы без администратора — до кода
  [ ] build / clippy / test (+ тест на разбор вывода powercfg)
  [ ] версия: minor
  [ ] README: что именно можно переключать и на каких системах
  [ ] CLAUDE.md: строка в Правило 6 про powercfg и overlay-схемы Windows 11
  [ ] UI: настольная машина и ноутбук, система без нужных прав, Linux/macOS
      с заглушкой, 520x420


################################################################################
ЗАДАЧА 20: Запоминать и галочки «вшить в файл»
################################################################################
Объём:            S
Приоритет:        средний
В планах README:  нет
Слои/файлы:       src/engine/settings.rs (Settings, to_json, parse),
                  src/app.rs (SavioApp::new — сид, embed_options — remember)

ЗАЧЕМ
  Задача 7 научила Savio помнить формат, качество и папку, но галочки
  «Метаданные / Обложку ролика / Субтитры» по-прежнему сбрасываются при каждом
  запуске. Для того, кто качает музыку и всегда вшивает обложку с названием,
  это ровно та же ежедневная морока, ради которой затевалась задача 7, — и
  сейчас получается непоследовательно: формат помним, а соседнюю строчку в
  той же карточке нет.

  Отдельной задачей, а не частью седьмой, потому что там был назван точный
  список полей (format, out_dir, quality), и расширять его молча нельзя:
  решение «помнить ли включённой галочку, которая молча ничего не сделает без
  ffmpeg» стоит принять отдельно, см. ловушки.

ЧТО СДЕЛАТЬ
  Добавить в `Settings` (settings.rs:51) поле `options: DownloadOptions`,
  писать три флага в `to_json` (settings.rs:148) и читать их в `parse`
  (settings.rs:171). В `SavioApp::new` (app.rs:421) сеять `options` оттуда же,
  а в `embed_options` (app.rs:1298) — позвать `self.remember()` там, где
  галочка изменилась (`checkbox` уже возвращает `Response`, изменение видно
  по `.changed()`).

ТОЧКИ РАСШИРЕНИЯ
  Settings (settings.rs:51), to_json (settings.rs:148), parse (settings.rs:171),
  SavioApp::new (app.rs:421), SavioApp::remember (app.rs:537),
  embed_options (app.rs:1298), DownloadOptions (model.rs:300).

  Рядом лежит однотипная, но НЕ входящая сюда настройка — источник cookies
  (задача 8). Она намеренно не запоминается; отдельно про неё — задача 24.

СОБЛЮДЕНИЕ АРХИТЕКТУРЫ
  Ничего нового: тот же файл, тот же писатель, тот же доменный тип. Границы
  слоёв не двигаются, зависимостей не прибавляется.

РИСКИ И ЛОВУШКИ
  - Восстановленная галочка без ffmpeg. Сами по себе ключи `--embed-*` в
    командную строку без ffmpeg не попадают (см. Правило 6), так что загрузка
    не сорвётся, но пользователь при каждом запуске будет видеть жёлтую
    оговорку «Вшивать нечем» под галочками, которые сам он в этом сеансе не
    ставил. Решить заранее: гасить ли восстановленные галочки, когда
    `ffmpeg_missing`, — и если гасить, то не записывать это обратно в файл,
    иначе один запуск без ffmpeg сотрёт настройку навсегда.
  - `embed_subs` живёт только у MP4. Если сохранён MP3 с включёнными
    субтитрами, при старте галочка окажется выключенной по формату, но в
    файле останется взведённой — это правильно (вернутся при переключении на
    MP4), но проверить надо глазами: переключение формата не должно писать
    в файл значение, которого пользователь не выбирал.
  - `checkbox` (app.rs:2328) сейчас отдаёт `Response` наружу ради
    `on_disabled_hover_text`. Звать `remember` по `.changed()` — три вызова
    подряд; вынести их в одно место (после всех трёх галочек, по «или»
    изменений), чтобы не забыть четвёртую, когда она появится.

ГОТОВНОСТЬ
  [ ] build / clippy / test (+ round-trip в settings.rs на новое поле)
  [ ] версия: minor
  [ ] README: строка про запоминание — дополнить перечень
  [ ] CLAUDE.md: не требуется
  [ ] UI: запуск с сохранёнными галочками при наличии и при отсутствии ffmpeg,
      переключение MP4 ↔ MP3 с включёнными субтитрами, 520x420


################################################################################
ЗАДАЧА 21: История, переживающая перезапуск, и кнопка «Очистить»
################################################################################
Объём:            M
Приоритет:        средний
В планах README:  нет (пункт «История загрузок» закрыт задачей 9)
Слои/файлы:       src/engine/settings.rs или новый src/engine/history.rs
                  (чтение и запись файла), src/app.rs (History::remember,
                  history_tab — кнопка «Очистить»)

ЗАЧЕМ
  Задача 9 сделала историю за текущий запуск: список живёт в памяти и при
  закрытии окна пропадает. Ровно тот вопрос, ради которого вкладка и
  заводилась («куда легло то, что я качал вчера?»), она при этом не
  закрывает — вчерашний запуск был другим. Найти файл через неделю по-прежнему
  можно только поиском по диску.

  Отдельной задачей, а не частью девятой, потому что там были названы слой и
  файл (только app.rs, накопитель в памяти), а сохранение на диск — это уже
  движок, схема файла и её совместимость.

ЧТО СДЕЛАТЬ
  Дописывать историю в файл рядом с `settings.json` и читать её при старте.
  Взять готовый образец — `settings.rs`: тот же каталог (`binaries::app_dir`),
  та же запись через временный файл с `rename`, то же правило «любая неудача
  молча откатывается к пустому значению». Своим файлом (`history.json`), а не
  полем в настройках: у списка другой ритм записи (после каждой загрузки, а не
  по дебаунсу щелчков) и другой размер, и мешать их в одном файле — значит
  переписывать настройки при каждом скачанном ролике.

  Вместе с этим — кнопка «Очистить историю» в `history_tab` (app.rs:2169).
  Пока список живёт до закрытия окна, без неё можно обойтись: закрыл Savio —
  список пуст. Как только он переживает перезапуск, кнопка становится
  обязательной: перечень скачанного видит любой, кто откроет программу на этом
  компьютере, и стереть его должно быть можно, не разыскивая файл руками.
  По той же причине очистка обязана стирать и файл на диске, а не только `Vec`.

ТОЧКИ РАСШИРЕНИЯ
  History (app.rs:132), History::remember (app.rs:143), history_tab
  (app.rs:2169), HISTORY_LIMIT (app.rs:107), SavioApp::new (app.rs — сид
  истории оттуда же, где `settings::load`), Saver (settings.rs:225) как
  образец фонового писателя.

СОБЛЮДЕНИЕ АРХИТЕКТУРЫ
  Чтение и запись — движок, как и `settings.rs`; UI по-прежнему не касается
  диска. Нового канала связи не появляется: история наполняется из того же
  `Event::Done`. `HistoryEntry` при этом переезжает в `model.rs` — доменный
  тип, который ходит через границу слоёв, в app.rs остаться не может.

РИСКИ И ЛОВУШКИ
  - Запись из кадра отрисовки. `Event::Done` разбирается в `drain_events`,
    то есть в `ui()`, и писать оттуда файл нельзя (Правило 1). Нужен фоновый
    писатель, как `settings::Saver`, — и `flush()` в `on_exit` по той же
    причине: после `on_exit` eframe зовёт `std::process::exit(0)`, и `Drop`
    не выполняется никогда.
  - Потолок. `HISTORY_LIMIT` = 50 сейчас защищает только память. У файла
    потолок нужен свой и по той же причине — иначе он растёт без предела;
    писать целиком (а не дописывать в конец) проще и надёжнее.
  - Записи, потерявшие файл. Через месяц половина путей ведёт в никуда.
    Проверять существование в кадре нельзя (диск в `ui()`), а проверять при
    старте — это `HISTORY_LIMIT` обращений к файловой системе до первого
    кадра. Решить заранее: не проверять вовсе (проводник скажет сам, как
    сейчас) или проверять на своём потоке и помечать строку.
  - Приватность. Список скачанного — чувствительные данные, и до сих пор
    Savio не писал на диск ничего, кроме трёх настроек. Стоит сказать об этом
    в README (раздел «Требования» перечисляет всё, что программа пишет) и,
    возможно, спросить пользователя — выключенная по умолчанию память истории
    честнее включённой.

ГОТОВНОСТЬ
  [ ] build / clippy / test (+ round-trip файла истории и обрезка по потолку)
  [ ] версия: minor
  [ ] README: раздел «История загрузок» — про диск вместо «только в памяти»,
      раздел «Требования» — про второй файл рядом с settings.json
  [ ] CLAUDE.md: не требуется
  [ ] UI: история пустая / полная / с исчезнувшими файлами, кнопка «Очистить»,
      520x420


################################################################################
ЗАДАЧА 22: Двойная подсказка у обрезанных подписей  ⚠ дефект, не функционал
################################################################################
Объём:            S
Приоритет:        низкий
В планах README:  нет
Слои/файлы:       src/app.rs (пять мест с `.truncate()` + `.on_hover_text`)

ЗАЧЕМ
  Наведя курсор на обрезанную подпись, пользователь видит две всплывающие
  подсказки с одним и тем же текстом, одна под другой, и вдобавок разного
  вида: верхняя наследует стиль `RichText` подписи, нижняя рисуется обычным
  текстом. Выглядит как сбой отрисовки.

  Причина — молчаливое поведение egui, описанное в Правиле 4 CLAUDE.md: у
  `Label` включён по умолчанию `show_tooltip_when_elided`, и обрезанная метка
  сама вешает подсказку с полным текстом. Свой `on_hover_text` её не
  заменяет, а добавляет вторую: egui считает подсказки на виджет
  (`PerWidgetTooltipState`, `tooltip_count`) и ставит каждую следующую ниже
  предыдущей. Проверено по исходникам egui 0.35
  (`widgets/label.rs:285-293`, `containers/tooltip.rs:113-152`) и опытом.

  Заодно там же второй, более мелкий изъян: когда текст **влез** целиком,
  ручной `on_hover_text` показывает подсказку, дословно повторяющую то, что
  и так видно.

ЧТО СДЕЛАТЬ
  Убрать ручной `.on_hover_text(...)` у подписей, которые показывают ровно
  свой же текст, — подсказку и так покажет egui:
    - `folder_row`, путь папки сохранения        (app.rs:1465)
    - `status_section`, название ролика          (app.rs:1547)
    - `status_section`, путь готового файла      (app.rs:1618)
    - `meta_file_row`, путь выбранного файла     (app.rs:1846)
    - `tags_modal`, имя тега                     (app.rs:2041)
  Во вкладке «История» (`history_card`) это уже сделано — там же лежит
  комментарий с причиной; сверяться стоит с ним.

  Если где-то подсказка **не** повторяет подпись, а поясняет её, штатную
  надо гасить явно: `Label::show_tooltip_when_elided(false)`, иначе останутся
  те же две коробки, просто с разным текстом.

ТОЧКИ РАСШИРЕНИЯ
  app.rs:1465, 1547, 1618, 1846, 2041; образец — history_card (app.rs:2197).

СОБЛЮДЕНИЕ АРХИТЕКТУРЫ
  Правка целиком в слое UI, по одной строке на место. Ни движка, ни модели,
  ни темы не касается.

РИСКИ И ЛОВУШКИ
  - Проверяется только глазами: ни сборка, ни clippy, ни тесты двойную
    подсказку не видят. Наводить надо именно на **обрезанную** строку —
    на короткой подсказки нет вовсе, и место выглядит исправным.
  - `tags_modal` (app.rs:2041) — единственное место, где подпись лежит в
    `add_sized` фиксированной ширины: там обрезается почти всегда, и проверять
    удобнее всего именно на нём.

ГОТОВНОСТЬ
  [ ] build / clippy / test
  [ ] версия: patch
  [ ] README: не требуется
  [ ] CLAUDE.md: не требуется (правило уже добавлено в Правило 4)
  [ ] UI: наведение на обрезанную и на короткую подпись в каждом из пяти мест


################################################################################
ЗАДАЧА 23: Файл cookies.txt как второй источник входа
################################################################################
Объём:            S
Приоритет:        средний
В планах README:  нет
Слои/файлы:       src/model.rs (CookieSource),
                  src/engine/ytdlp.rs (push_cookies),
                  src/app.rs (cookie_selector + выбор файла)

ЗАЧЕМ
  Задача 8 дала список браузеров, но на Windows из него реально работает
  только Firefox: Chrome и всё, что на нём основано (Edge, Brave, Opera,
  Vivaldi), в свежих версиях шифрует cookies так, что снаружи их не прочитать
  («Failed to decrypt with DPAPI», проверено вживую 2026-07-27). Пользователю
  Chrome сейчас сказать нечего, кроме «поставьте Firefox».

  Выход у него есть, и он общепринятый: расширение вроде «Get cookies.txt»
  выгружает cookies в текстовый файл формата Netscape, и yt-dlp принимает его
  ключом `--cookies <файл>`. Это ровно тот же вход в аккаунт, только добытый
  средствами самого браузера.

ЧТО СДЕЛАТЬ
  Добавить в `CookieSource` (model.rs:223) вариант `File`, несущий путь, —
  либо, если не хочется терять `Copy`, отдельное поле `cookie_file` в
  `Request` рядом с `cookies`. В `push_cookies` (ytdlp.rs:51) выдать
  `--cookies <путь>` вместо `--cookies-from-browser`. В UI — пункт списка
  «Из файла…», открывающий `rfd::FileDialog` (образец — `folder_row`,
  app.rs:1442), и строка с выбранным путём под списком.

ТОЧКИ РАСШИРЕНИЯ
  CookieSource (model.rs:223), push_cookies (ytdlp.rs:51),
  cookie_selector (app.rs:1378), Request.cookies (model.rs:319).

СОБЛЮДЕНИЕ АРХИТЕКТУРЫ
  Ещё один аргумент в той же функции сборки; чтение файла — работа yt-dlp,
  на своём потоке. Ни нового канала, ни зависимостей.

РИСКИ И ЛОВУШКИ
  - Формат файла проверяет yt-dlp, а не мы. На постороннем файле он отвечает
    «does not look like a Netscape format cookies file» — примету стоит
    добавить в FAILURE_HINTS (ytdlp.rs:258) вместе с советом, чем такой файл
    выгружают.
  - Путь к файлу — это личные данные пользователя. В журнал уходит вся
    командная строка (`Event::Log` в mod.rs:142), то есть путь окажется
    в тексте, который человек копирует кнопкой и шлёт в сообщение о проблеме.
    Решить заранее, показывать ли его целиком.
  - Файл живёт дольше сеанса, в отличие от выбранного браузера, поэтому
    вопрос «запоминать ли» здесь острее — см. задачу 24.

ГОТОВНОСТЬ
  [ ] build / clippy / test (+ тест: `--cookies` и `--cookies-from-browser`
      никогда не попадают в одну командную строку)
  [ ] версия: minor
  [ ] README: дополнить раздел «Закрытые ролики»
  [ ] CLAUDE.md: не требуется (правило про cookies уже есть в Правиле 6)
  [ ] UI: файл выбран / не выбран / выбран посторонний файл, 520x420


################################################################################
ЗАДАЧА 24: Запоминать ли выбранный источник cookies
################################################################################
Объём:            S
Приоритет:        низкий, но решение нужно до задачи 23
В планах README:  нет
Слои/файлы:       src/engine/settings.rs, src/app.rs

ЗАЧЕМ
  Задача 8 намеренно НЕ запоминает выбранный браузер: при каждом запуске
  список стоит на «Не использовать». Это решение, а не недоделка, и принято
  оно так: доступ к профилю браузера — не то же самое, что формат файла,
  и включаться сам собой через неделю после того, как понадобился один раз,
  он не должен.

  Обратная сторона видна сразу: тот, кто качает с закрытого сайта постоянно,
  выбирает браузер при каждом запуске. Ровно та морока, ради которой
  затевалась задача 7.

  Записано отдельной задачей, потому что это выбор человека, а не вывод из
  кода: обе стороны разумны, и молча поменять умолчание нельзя ни в ту,
  ни в другую сторону.

ЧТО СДЕЛАТЬ (если решено запоминать)
  Добавить в `Settings` (settings.rs:51) поле `cookies: CookieSource`
  с парой `*_token`/`*_from_token` рядом с `format_token` (settings.rs:104)
  и `quality_token` (settings.rs:124), сеять его в `SavioApp::new`
  (app.rs:421) и звать `self.remember()` из `cookie_selector` (app.rs:1378)
  по `.changed()` у списка.

РИСКИ И ЛОВУШКИ
  - Неизвестный токен обязан откатываться к `None`, а не к первому браузеру
    в списке: файл от будущей версии не должен молча включать чтение чужого
    профиля. Это уже так работает у формата и качества (settings.rs:171) —
    важно не отступить от правила.
  - Если делать вместе с задачей 23, в файле окажется ещё и путь к файлу
    cookies. Тогда это уже не «настройка», а след того, чем пользователь
    занимался, — и вопрос про запоминание надо решать заново.

ГОТОВНОСТЬ
  [ ] build / clippy / test (+ round-trip на новое поле)
  [ ] версия: patch или minor — по решению
  [ ] README: строка про запоминание — дополнить перечень и абзац в разделе
      «Закрытые ролики», где сейчас прямо написано обратное
  [ ] CLAUDE.md: не требуется
  [ ] UI: перезапуск с сохранённым браузером


================================================================================
КОНЕЦ РЕЕСТРА
================================================================================
